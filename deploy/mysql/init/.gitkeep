# IOP_Library.sql is generated by deploy/scripts/migrate-mysql.sh and mounted
# into the mysql container's /docker-entrypoint-initdb.d for first-boot restore.
# The dump is intentionally git-ignored (large, contains data).
